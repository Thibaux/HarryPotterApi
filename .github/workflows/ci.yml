name: CI

on:
  push:
    branches: [ main ]

jobs:
  runTests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Install dependencies
        run: npm ci

      - name: Run API server
        run: npm run start
        
      - name: Run tests
        run: npm run test
